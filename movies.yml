movie:
  directory: C:\\tmp\\movies
  file-pattern: "*.txt"
  prevent-duplicates: true
  fixed-delay: 5000
  logging:
    level:
      com:
        apress:
          cloud: DEBUG

eureka:
  client:
    register-with-eureka: false
    service-url:
      default-zone: http://localhost:8761/eureka/
